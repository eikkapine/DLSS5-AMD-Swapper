<Window x:Class="Dlss5AmdSwapper.SetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up Neural Rendering" Width="560" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" FontSize="12"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <StackPanel Margin="24">
        <TextBlock Text="{Binding GameName}" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock Text="Choose how Neural Rendering reaches this game." Foreground="{DynamicResource MutedBrush}" Margin="0,4,0,16"/>

        <TextBlock Text="ROUTE" Foreground="{DynamicResource MutedBrush}" FontSize="10" FontWeight="Bold"/>
        <RadioButton x:Name="PreSrRadio" GroupName="Route" Margin="0,8,0,0" Foreground="{DynamicResource TextBrush}" IsEnabled="{Binding PreSrAvailable}">
            <StackPanel><TextBlock Text="OptiScaler pre-SR (render-resolution neural pass)" FontWeight="SemiBold"/><TextBlock Text="{Binding PackageSummary}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/><TextBlock Text="{Binding WeightsSummary}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/></StackPanel>
        </RadioButton>
        <RadioButton x:Name="PostFsrRadio" GroupName="Route" Margin="0,10,0,0" Foreground="{DynamicResource TextBrush}">
            <StackPanel><TextBlock Text="Official AMD runtime (DLSS-NR-on-AMD setup)" FontWeight="SemiBold"/><TextBlock Text="Uses the official runtime's mode. Version 0.3 defaults to pre-upscale." Foreground="{DynamicResource MutedBrush}" FontSize="10"/></StackPanel>
        </RadioButton>

        <TextBlock Text="PRE-SR PRESET" Foreground="{DynamicResource MutedBrush}" FontSize="10" FontWeight="Bold" Margin="0,18,0,0"/>
        <RadioButton x:Name="LightRadio" GroupName="Preset" Margin="0,8,0,0" Foreground="{DynamicResource TextBrush}" IsEnabled="{Binding ElementName=PreSrRadio, Path=IsChecked}">
            <StackPanel><TextBlock Text="Light" FontWeight="SemiBold"/><TextBlock Text="One neural pass, gentle structure. Quality scaling (1.5x): the cheapest setting, closest to the game's native image." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/></StackPanel>
        </RadioButton>
        <RadioButton x:Name="BalancedRadio" GroupName="Preset" Margin="0,8,0,0" Foreground="{DynamicResource TextBrush}" IsEnabled="{Binding ElementName=PreSrRadio, Path=IsChecked}">
            <StackPanel><TextBlock Text="Balanced (recommended)" FontWeight="SemiBold"/><TextBlock Text="One neural pass at moderate structure. Balanced scaling (1.7x)." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/></StackPanel>
        </RadioButton>
        <RadioButton x:Name="DetailRadio" GroupName="Preset" Margin="0,8,0,0" Foreground="{DynamicResource TextBrush}" IsEnabled="{Binding ElementName=PreSrRadio, Path=IsChecked}">
            <StackPanel><TextBlock Text="Detail" FontWeight="SemiBold"/><TextBlock Text="Two neural passes at full structure. Performance scaling (2.0x). Each extra pass costs GPU frame time." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/></StackPanel>
        </RadioButton>
        <RadioButton x:Name="MaxRadio" GroupName="Preset" Margin="0,8,0,0" Foreground="{DynamicResource TextBrush}" IsEnabled="{Binding ElementName=PreSrRadio, Path=IsChecked}">
            <StackPanel><TextBlock Text="Max" FontWeight="SemiBold"/><TextBlock Text="Three neural passes and Ultra Performance scaling (3.0x). The most expensive option, and the lowest internal resolution; raising every slider further usually looks worse, not better." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10"/></StackPanel>
        </RadioButton>

        <TextBlock Text="SCALING (INTERNAL RESOLUTION)" Foreground="{DynamicResource MutedBrush}" FontSize="10" FontWeight="Bold" Margin="0,18,0,0"/>
        <ComboBox x:Name="ScalingCombo" ItemsSource="{Binding ScalingChoices}" Width="220" HorizontalAlignment="Left" Margin="0,8,0,0"
                  IsEnabled="{Binding ElementName=PreSrRadio, Path=IsChecked}"/>
        <TextBlock Text="A tier forces the render resolution and replaces the upscaler quality chosen inside the game. Game controlled leaves that setting alone." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" FontSize="10" Margin="0,6,0,0"/>

        <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,18,0,0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers><DataTrigger Binding="{Binding ElementName=PreSrRadio, Path=IsChecked}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel>
                <TextBlock Text="After installation" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ActivationGuidance}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,5,0,0" FontSize="11"/>
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button Content="Cancel" MinWidth="90" Style="{StaticResource GhostButton}" IsCancel="True" Click="Cancel_Click" Margin="0,0,10,0"/>
            <Button Content="Set up" MinWidth="110" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Confirm_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
